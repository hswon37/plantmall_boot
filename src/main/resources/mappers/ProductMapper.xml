<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.plantmall.dao.mybatis.mapper.ProductMapper">

  <cache />

  <select id="getProduct" resultType="Product">
    SELECT
      PRODUCTID,
      CATEGORYID,
      USERID,
      pNAME as p_name,
      DESCRIPTION,
      PRICE,
      PRODUCTIMG
    FROM PRODUCT
    WHERE PRODUCTID = #{productId}
  </select>

  <select id="getProductListByCategory" resultType="Product">
    SELECT
      PRODUCTID,
      CATEGORYID,
      USERID,
      pNAME as p_name,
      DESCRIPTION,
      PRICE,
      PRODUCTIMG
    FROM PRODUCT
    WHERE CATEGORYID = #{value}
  </select>

  <select id="searchProductList" resultType="Product">
    SELECT
      PRODUCTID,
      CATEGORYID,
      USERID,
      pNAME as p_name,
      DESCRIPTION,
      PRICE,
      PRODUCTIMG
    FROM PRODUCT
    WHERE lower(name) like #{value}
  </select>
  
  <select id="showProductList" resultType="Product">
    SELECT
      PRODUCTID,
      CATEGORYID,
      USERID,
      pNAME as p_name,
      PRICE,
      PRODUCTIMG,
      USERNAME
    FROM PRODUCT, USER_TABLE
    WHERE PRODUCTID = #{productId}
    AND PRODUCT.USERID = USER_TABLE.USERID
  </select>
  
  <insert id="insertProduct" parameterType="com.example.plantmall.domain.Product">
		<selectKey keyProperty="id" resultType="int" order="BEFORE">
			SELECT Product_seq.nextval AS id FROM DUAL
		</selectKey>
		INSERT INTO Product (productid, categoryid, pname, description, price, productimg, userid)
		VALUES (#{productid}, #{categoryid}, #{pname}, #{description}, #{price}, #{productimg}, #{userid})
		  
  </insert>

</mapper>